
import 'package:flutter/material.dart';

class AppMenuItem {
  String name;
  IconData icon;
  String route;

  AppMenuItem({
    required this.name,
    required this.icon,
    required this.route,
  });
}