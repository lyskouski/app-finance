name: Flutter/Dart Quality Gates (Pull-Request, Branches)

on:
  push:
    branches-ignore:
      - master
  pull_request:

permissions: read-all

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true
      - run: flutter --version

      - name: Install Dependencies
        run: flutter pub get

      - name: Verify Formatting
        if: matrix.os == 'ubuntu-latest'
        run: dart format -l 120 --output=none --set-exit-if-changed .

      - name: Check Localizations Ordering
        if: matrix.os == 'ubuntu-latest'
        run: dart run grinder sort-translations

      # Consider passing '--fatal-infos' for slightly stricter analysis.
      #- name: Analyze Project Source
      #  run: flutter analyze

      - name: Run All Tests
        run: dart run grinder test > test_execution.log
        
      - name: Generate Golden Images
        if: ${{ failure() }}
        run: flutter test --update-goldens test/widget

      - name: Create Dependency Graph
        if: matrix.os == 'ubuntu-latest'
        run: |
          dart pub global activate lakos
          dart run grinder dependency-graph
          dot -Tsvg coverage/dependencies.dot -o coverage/dependencies.svg

      - name: Store Tests Artifacts
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: testing_logs--${{ matrix.os }}
          path: |
            coverage
            test/widget
            test_execution.log
